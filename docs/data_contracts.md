# Data Contracts

Core contracts will be implemented as Pydantic models in `src/ebrag/schemas`.

Phase 0 only defines the package structure. The required contracts are added in
Phase 2, after the database models establish stable identifiers.
